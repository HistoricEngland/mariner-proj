{
  "defaults": {
    "standard": "WCAG2AA",
    "runner": "axe",
    "timeout": 60000,
    "wait": 1500,
    "chromeLaunchConfig": {
      "args": [
        "--no-sandbox",
        "--disable-dev-shm-usage",
        "--disable-gpu"
      ]
    }
  },
  "urls": [
    "http://127.0.0.1:8000/"
  ]
}